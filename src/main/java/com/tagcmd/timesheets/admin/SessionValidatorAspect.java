package com.tagcmd.timesheets.admin;

public class SessionValidatorAspect {
	// Redirect user to show worklog if session exists
	// Check whether the user is in session
    // Check whether the user in url is same as user in session
}
